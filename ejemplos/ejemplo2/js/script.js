"use strict";

const texto = document.getElementById("texto");
const invertir = document.getElementById("invertir");
const salida = document.getElementById("salida");

invertir.addEventListener("click", invertirCadena);
